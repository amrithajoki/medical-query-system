services:
  - type: web
    name: medical-query-api
    env: python
    buildCommand: pip install -r requirements.txt && python ml_model/trainer.py
    startCommand: uvicorn api.fastapi_app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0